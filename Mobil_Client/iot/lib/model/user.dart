class User {
  User(this.id, this.firstName, this.lastName, this.username, this.token, this.isAdmin);
  int id;
  String firstName;
  String lastName;
  String username;
  String token;
  bool isAdmin;
}